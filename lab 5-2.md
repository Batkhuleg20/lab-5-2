# Ухаалаг авто зогсоолын системийн класс диаграм

```mermaid
classDiagram
    class Хэрэглэгч {
        +хэрэглэгчийнID: String
        +нэр: String
        +и-мэйл: String
        +утасны_дугаар: String
        +нууц_үг: String
        +төлбөрийн_мэдээлэл: String
        +машины_дугаар: String
        +бүртгүүлэх()
        +нэвтрэх()
        +мэдээллээ_шинэчлэх()
        +түүх_харах()
    }

    class Зогсоол {
        +зогсоолынID: String
        +байршил: String
        +нийт_суудал: Integer
        +боломжит_суудал: Integer
        +ажиллах_цаг: String
        +үнийн_тариф: Double
        +төлөв: String
        +мэдээлэл_авах()
        +суудал_шалгах()
        +тариф_тооцох()
    }

    class ЗогсоолынБайр {
        +байрынID: String
        +зогсоолынID: String
        +төлөв: String
        +төрөл: String
        +мэдрэгч_төлөв: Boolean
        +төлөв_шинэчлэх()
        +сул_эсэхийг_шалгах()
    }

    class Захиалга {
        +захиалгынID: String
        +хэрэглэгчийнID: String
        +зогсоолынID: String
        +байрынID: String
        +орсон_цаг: DateTime
        +гарсан_цаг: DateTime
        +төлөв: String
        +нийт_үнэ: Double
        +үүсгэх()
        +цуцлах()
        +дуусгах()
        +үргэлжлэх_хугацаа_тооцох()
    }

    class Төлбөр {
        +төлбөрийнID: String
        +захиалгынID: String
        +хэрэглэгчийнID: String
        +дүн: Double
        +төлбөрийн_арга: String
        +төлсөн_огноо: DateTime
        +статус: String
        +төлөх()
        +буцаан_олгох()
        +баримт_үүсгэх()
    }

    class Мэдрэгч {
        +мэдрэгчийнID: String
        +байрынID: String
        +төрөл: String
        +төлөв: Boolean
        +суурилуулсан_огноо: Date
        +төлөв_илгээх()
        +шалгах()
        +засварлах()
    }

    class Камер {
        +камерынID: String
        +зогсоолынID: String
        +байршил: String
        +төрөл: String
        +төлөв: String
        +дүрс_бичих()
        +машины_дугаар_таних()
        +хянах()
    }

    class Систем {
        +системийнID: String
        +хувилбар: String
        +сүүлд_шинэчилсэн: DateTime
        +тохиргоо: String
        +мэдээлэл_боловсруулах()
        +тайлан_гаргах()
        +бүртгэл_хөтлөх()
    }

    class Админ {
        +админID: String
        +нэр: String
        +и-мэйл: String
        +утас: String
        +эрх: String
        +нэвтрэх()
        +систем_удирдах()
        +тохиргоо_өөрчлөх()
        +хэрэглэгч_удирдах()
    }

    class МашинТаних {
        +танихID: String
        +камерынID: String
        +машины_дугаар: String
        +орсон_цаг: DateTime
        +гарсан_цаг: DateTime
        +зураг: String
        +дугаар_таних()
        +бүртгэл_үүсгэх()
        +тулгалт_хийх()
    }

    Хэрэглэгч "1" -- "0..*" Захиалга: include
    Хэрэглэгч "1" -- "0..*" Төлбөр: include
    Зогсоол "1" -- "1..*" ЗогсоолынБайр: include
    Зогсоол "1" -- "0..*" Камер: include
    Зогсоол "1" -- "0..*" Захиалга: include
    ЗогсоолынБайр "1" -- "0..1" Мэдрэгч: include
    ЗогсоолынБайр "1" -- "0..*" Захиалга: extend
    Захиалга "1" -- "0..1" Төлбөр: include
    Камер "1" -- "0..*" МашинТаних: extend
    Систем -- Зогсоол: include
    Систем -- Хэрэглэгч: include
    Админ -- Систем: include
